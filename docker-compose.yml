services:
    node:
        image: bcnmy/mee-node:1.1.94
        restart: on-failure
        env_file: 
          - .env
        ports:
          - "3000:3000"
        links:
          - redis
        volumes:
          - ./logs:/usr/src/app/logs
          - ./keystore:/usr/src/app/keystore
          - ./chains-local:/usr/src/app/chains
    redis:
        image: redis:alpine
        restart: always
        command: redis-server --dir /data --save 60 1 --appendonly yes --loglevel warning
        volumes:
          - ./redis-data:/data
